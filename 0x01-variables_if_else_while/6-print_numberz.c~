#include <stdio.h>
#include <assert.h>
/**
 *main-function begins
 *
 *Return: function ends
 */
int main(void)
{
	int i = 0;
	while (i < 10)
	{
		putchar("%d", i);
		i++;
	}
	putchar('\n');
	return (0);
}
